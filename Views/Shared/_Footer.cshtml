<footer class="bg-white border-top py-5">
    <div class="container">
        <div class="row">
            <div class="col-md-4 mb-4">
                <div class="d-flex align-items-center gap-2 mb-3">
                    <i class="fas fa-ship text-primary fa-lg"></i>
                    <span class="h5 mb-0 fw-bold">CargoCaptain</span>
                </div>
                <p class="text-muted small mb-0">
                    CargoClear OS is a high-performance terminal infrastructure that digitizes manifests, ocean freight operations, customs declarations, and multi-currency invoicing.
                </p>
            </div>
            <div class="col-md-2 mb-4">
                <h6 class="fw-bold mb-3">Modules</h6>
                <ul class="list-unstyled small d-flex flex-column gap-2">
                    <li><a href="#" class="text-decoration-none text-muted">Bookings & B/L</a></li>
                    <li><a href="#" class="text-decoration-none text-muted">Vessel Manifests</a></li>
                    <li><a href="#" class="text-decoration-none text-muted">Customs Declarations</a></li>
                    <li><a href="#" class="text-decoration-none text-muted">Multi-currency Invoices</a></li>
                </ul>
            </div>
            <div class="col-md-2 mb-4">
                <h6 class="fw-bold mb-3">Infrastructure</h6>
                <ul class="list-unstyled small d-flex flex-column gap-2">
                    <li><a href="#" class="text-decoration-none text-muted">Terminal Ops</a></li>
                    <li><a href="#" class="text-decoration-none text-muted">Container Staging</a></li>
                    <li><a href="#" class="text-decoration-none text-muted">Gate Operations</a></li>
                    <li><a href="#" class="text-decoration-none text-muted">Developer API</a></li>
                </ul>
            </div>
            <div class="col-md-4 mb-4">
                <h6 class="fw-bold mb-3">System Time (UTC)</h6>
                <p class="text-muted small mb-1">
                    <i class="far fa-clock me-1"></i> <span id="footer-utc-time">2026-07-19 10:11:00</span>
                </p>
                <p class="text-muted small">
                    <i class="fas fa-network-wired me-1"></i> Status: <span class="text-success fw-bold">Fully Operational</span>
                </p>
            </div>
        </div>
        <hr class="my-4 text-muted opacity-25">
        <div class="d-flex flex-column flex-sm-row justify-content-between align-items-center gap-3">
            <span class="text-muted small">&copy; 2026 CargoCaptain Inc. All rights reserved.</span>
            <div class="d-flex gap-3">
                <a href="#" class="text-muted small text-decoration-none">Terms of Service</a>
                <a href="#" class="text-muted small text-decoration-none">Privacy Policy</a>
            </div>
        </div>
    </div>
</footer>

<script>
    // Live update footer clock to UTC
    function updateFooterClock() {
        const utcDate = new Date();
        const yyyy = utcDate.getUTCFullYear();
        const mm = String(utcDate.getUTCMonth() + 1).padStart(2, '0');
        const dd = String(utcDate.getUTCDate()).padStart(2, '0');
        const hh = String(utcDate.getUTCHours()).padStart(2, '0');
        const min = String(utcDate.getUTCMinutes()).padStart(2, '0');
        const ss = String(utcDate.getUTCSeconds()).padStart(2, '0');
        
        const formatted = `${yyyy}-${mm}-${dd} ${hh}:${min}:${ss}`;
        const clockEl = document.getElementById('footer-utc-time');
        if (clockEl) {
            clockEl.textContent = formatted;
        }
    }
    setInterval(updateFooterClock, 1000);
    updateFooterClock();
</script>
